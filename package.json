{
  "name": "vue-project",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "dev": "parcel index.html --no-cache",
    "watch": "parcel watch index.html --no-cache",
    "build": "parcel build index.html --out-dir build --public-url ./  --no-cache",
    "clean": "rimraf .cache build dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pym.js": "^1.3.1",
    "vue-analytics": "^5.8.0"
  },
  "devDependencies": {
    "autoprefixer": "^7.2.5",
    "babel-preset-env": "^1.6.1",
    "node-sass": "^4.7.2",
    "parcel-bundler": "parcel-bundler/parcel#vue",
    "postcss-modules": "^1.1.0",
    "rimraf": "^2.6.2",
    "vue": "^2.5.13",
    "vue-router": "^3.0.1",
    "vuex": "^3.0.1",
    "vuex-router-sync": "^5.0.0"
  }
}
